const { app, BrowserWindow } = require('electron'); app.whenReady().then(() => { let win = new BrowserWindow({show: false, webPreferences: {offscreen: true}}); win.loadFile('C:/Users/toygu/AppData/Local/Temp/35 MO TA CONG VIEC CHI TIET THEO VI TRI_VIE_HVHR_2025_ver01.html'); win.webContents.on('did-finish-load', async () => { console.log('Loaded HTML'); const size = await win.webContents.executeJavaScript('({w: document.body.scrollWidth, h: document.body.scrollHeight})'); console.log(size); app.quit(); }); });
